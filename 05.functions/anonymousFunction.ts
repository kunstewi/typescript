// Create an anonymous function assigned to a variable divide, that divides two numbers and returns the result.
// Call it with (10, 2) and log the output.

const divide = function (a: number, b: number): number {
  return a / b;
};

console.log(divide(10, 2))