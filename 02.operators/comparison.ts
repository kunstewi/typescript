// Create a function isAdult(age: number): boolean that returns:

// true if age >= 18

// false otherwise.

// Then test it with multiple inputs and use both == and === to observe any difference.

function isAdult(age:number): boolean {
    return age >= 18;
}

