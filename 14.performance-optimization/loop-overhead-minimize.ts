// you have this code:

// const sum = arr.reduce((acc, val) => acc + val, 0);
// const avg = sum / arr.length;
// const max = Math.max(...arr);


// This code iterates multiple times.
// Refactor it to compute sum, avg, and max in a single pass, and discuss the performance improvement.