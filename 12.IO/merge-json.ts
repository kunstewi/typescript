// Merge Multiple JSON Files

// Write a script that takes multiple JSON file paths as command-line arguments and merges all arrays inside them into a single merged.json.
// Example:

// node merge.js data1.json data2.json


// Output combined JSON with no duplicates based on a unique key (e.g., id).