// Given a data.json file containing an array of user objects, read and parse it into a TypeScript interface User { id: number; name: string; email: string; }.

// Then, log all user emails to the console.