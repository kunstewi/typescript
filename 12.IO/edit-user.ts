// Create a command-line tool edit-user.ts that:

// node edit-user.js update 3 name=John

// Loads users.json, finds the user with id=3, updates the name, and writes it back to the file.

// Use TypeScript interfaces for strong typing.
