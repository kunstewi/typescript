// Write a TypeScript script calc.ts that accepts three command-line arguments:

// node calc.js add 5 10


// and performs the corresponding operation (add, sub, mul, div).

// Validate arguments and print errors for invalid commands.