// Write a TypeScript function saveUsers(users: User[]): void that converts a list of users into JSON and saves it to users.json.

// Make sure to pretty-print the JSON with indentation.

// Catch serialization or file write errors.