// Type narrowing with error catching:
// Write a function safeToUpper(str: any) that:

// Tries to return str.toUpperCase().

// Catches errors (for non-string inputs) and returns "Invalid input" instead.