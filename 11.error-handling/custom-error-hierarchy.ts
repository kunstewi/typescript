// Custom error hierarchy:

// Create a base class AppError and two subclasses: NetworkError and DatabaseError.

// Write a function simulateOperation(type: string) that throws one of these errors based on type.

// Use a try/catch to handle each error type differently.