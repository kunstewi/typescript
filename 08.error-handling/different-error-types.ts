// Throwing different error types:
// Write a function processData(data: unknown) that:

// Throws "Data is null" if data is null.

// Throws "Data is not an object" if data is not an object.

// Otherwise logs "Processing data...".
// Use try/catch to test all 3 cases.