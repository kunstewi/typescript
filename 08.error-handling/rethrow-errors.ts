// Re-throwing errors:
// Write a function handleInput(input: string) that:

// Throws an error if the input is empty.

// Catches it, logs "Caught error, rethrowing...", and then rethrows the same error.
// Call it from another function main() that catches and logs "Final handler: <message>".
