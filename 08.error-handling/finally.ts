// Finally block usage:
// Write a function readFile(filePath: string) that:

// Simulates reading a file (just log "Reading file...").

// Throws an error randomly (e.g., Math.random() < 0.5).

// Always logs "Closing file" in a finally block regardless of success or failure.