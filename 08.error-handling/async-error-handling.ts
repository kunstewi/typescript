// Async error handling:
// Write an async function fetchData() that:

// Uses Promise.reject(new Error("Network failure")) to simulate a failed API call.

// Uses try/catch to handle it and print "Caught async error: <message>".