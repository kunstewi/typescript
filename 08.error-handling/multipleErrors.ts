// Catching multiple possible errors:
// Create a function parseJSON(jsonString: string) that:

// Tries to parse the JSON string.

// If parsing fails, catch the error and return null while logging "Invalid JSON: <error message>".