// Create an interface Logger with methods logInfo(), logError().

// Implement it in classes ConsoleLogger and FileLogger (mock behavior).

// Use dependency injection: pass a logger to another class that uses it.