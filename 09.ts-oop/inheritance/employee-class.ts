// Create a base class Employee with a method getSalary().

// Extend it with Manager that adds a bonus and overrides getSalary() to include it.

// Use super() to call the parent constructor and method.