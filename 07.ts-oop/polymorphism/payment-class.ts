// Create a base class Payment with method processPayment(amount: number).

// Extend with CreditCardPayment and PayPalPayment that have different implementations.

// Write a function that processes a list of mixed payment objects.