// Create an overloaded function formatValue that:

// When given a number, returns "Value: <num>"

// When given a string, returns "String: <text>"

// Example:

// formatValue(10); // "Value: 10"
// formatValue("hello"); // "String: hello"


// Hint: use TypeScript overload syntax:

// function formatValue(value: number): string;
// function formatValue(value: string): string;